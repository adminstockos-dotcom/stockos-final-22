"use client";import { Suspense } from "react";import { useSearchParams } from "next/navigation";function PanelInner(){const p=useSearchParams();const e=p?.get("empresa")||"MAXIMA";return(<iframe src={`/PANEL-EMPRESA-FINAL-BLINDADO-6MODULOS.html?empresa=${encodeURIComponent(e)}`} style={{width:"100%",height:"100vh",border:"none"}}/>)}export default function PanelPage(){return(<Suspense fallback={<div>Cargando...</div>}><PanelInner/></Suspense>)}